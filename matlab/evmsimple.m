function mx=evmsimple(rx)


con =[   0.7071 + 0.7071i; ...
  -0.7071 + 0.7071i; ...
  -0.7071 - 0.7071i; ...
   0.7071 - 0.7071i];

evm = comm.EVM( ...
    'ReferenceSignalSource','Estimated from reference constellation', ...
    'ReferenceConstellation',con);

mx=step(evm,rx);
